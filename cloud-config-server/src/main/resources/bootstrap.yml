server:
  port: 8888
spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mckshub/spring-microservices-config-repo.git
          username: mckshub
          password: Vidya$2692
          search-paths:
          - '{application}'
          clone-on-start: true
          refresh-rate: 10


  profiles:
    active: dev