spring:
  application:
    name: user-management-service
  cloud:
    config:
      uri: http://localhost:8888
  main:
    banner-mode: off
    log-startup-info: false
  profiles:
    active:
      - dev
management:
  endpoint:
    refresh:
      enabled: true
  endpoints:
    enabled-by-default: true
