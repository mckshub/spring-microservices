spring:
  application:
    name: category-service
  cloud:
    config:
      uri: http://localhost:8888
  main:
    banner-mode: off
    log-startup-info: false
  profiles:
    active:
      - dev

  